# Spécifications des spots

Copie de `collecte-api-meteo/assets/spots_specs/` pour le panneau détail :
noms de zone, textes, liens Windguru / webcam / anémomètre, coordonnées de mise à l'eau.

Cette copie n’est pas rafraîchie par le workflow de traitement (qui ne pousse que `quotidien.json` et `last_update.json`). Après un changement de specs côté collecte, recopier les CSV ici.

